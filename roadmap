1. Дэшборд.


2. Доавить проект.

Получаем список параметров:
1. Список объектов
2. Что отслеживаем
3. Куда сохраняем.
4. Как сохраняем - сразу, через Н минут/часов?


3. изменить проект.

4. Профиль.

5. Оплата.

6. подключение рекламного кабинета.





Добавление:
Поля


Основной скрипт

Загрузили настройки из файла(из базы)  
Загрузили из базы настройки проекта и текущие данные def loadSettings(projId) return objS Actions olddata
Сделали запросы к апи по настройкам проекта. getDataFromVK(executeMethod,data): ...return newdata
Пойманные=Олд дата-ньюдата caught=newdata-olddata
Если нет изменений
    Пасс
Если есть изменения
   Пойманные в ретаргет. 
   Ньюдата в базу
   
   
   
   SELECT * FROM Orders WHERE Id=6
   
   
   BLOB